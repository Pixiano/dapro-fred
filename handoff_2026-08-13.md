# Handoff — 2026-08-13

Quick-read summary of today. Full detail: `session_2026-08-12_wakeword_changes.md` (Parts 11-14).

## Shipped today (all live, all pushed)

- **Vision wasn't offered as a tool** for phrasings without the exact word "screen" or the exact phrase "what am i looking at" ("look **at** my screen", "what I'm looking at"). Fixed the cue list in `orchestrator/intent.py`. — `4bd36da`, `480ac25`
- **Vision never got told what you actually asked.** `whats_on_screen()` took no arguments; now takes `question`, threaded through to the vision prompt. Verified live. — `91d779c`
- **Silent turn fix**: `generate_stream` could yield nothing on an unclosed `<think>` block (same bug `generate()` already had a fix for, this sibling didn't). Now falls back to a spoken message instead of dead air. — `cac12c8`
- **Cold model loads are now logged** (`event_log`, kind `llm_model_load`) — a 106s latency incident today had no visibility into why; won't be a mystery next time. — `cac12c8`
- **Wake-word live capture**: every real activation now saves a WAV (trigger phrase + followup) + a manifest line (`spoke_after`, `cancelled`, transcript, score/gain) to `Core/data/wakeword_training/live_captures/`. Tested live just now with 2 real + 1 deliberate-silent activation — all 3 logged correctly. — `da75118`

## Current state

- FRED restarted after every fix above; all live.
- Wake-word model: still the 20-clip real-positive experiment from 08-12 (see Part 6 of the full doc) — not reverted, holding up so far.
- `live_captures/` will keep accumulating from normal use. Nothing consumes it yet — that's the natural next step once there's enough.

## Watch for

- Wake-word false-positive rate (`Core/data/wakeword_log.jsonl`) — the 20-clip model is unvalidated long-term.
- Any more cue-coverage misses on vision ("check X on my screen" phrasings not yet seen).
- Cerebras 429 frequency — every fix so far survives it gracefully, none reduce how often it happens.
